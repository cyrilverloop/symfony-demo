{% extends 'base.html.twig' %}

{% set loader = true %}


{# SEO #}

{% set pageHeadTitle = 'Chair' %}


{# Assets #}

{% block importmap %}
    {{ importmap([
        'app',
        'loader',
        'product-delete',
        'product-price-history'
    ]) }}
{% endblock %}


{# Body #}

{% block main %}
    <div class="container">
        <h1>Chair</h1>

        <div class="card">
            <h5 class="card-header">{{ chair.name }}</h5>

            <div class="card-body">
                <p class="card-text">{{ chair.description }}</p>

                <ul class="list-group list-group-flush">
                    <li class="list-group-item">Price : <strong>{{ chair.price }}&nbsp;€</strong>.</li>
                </ul>

                <div class="form-check form-switch">
                    <input
                        aria-controls="price_history"
                        aria-expanded="false"
                        data-bs-target="#price_history"
                        data-bs-toggle="collapse"
                        class="form-check-input"
                        id="price_history_switch"
                        role="switch"
                        type="checkbox"
                    >

                    <label
                        class="form-check-label"
                        for="price_history_switch"
                    >
                        Price history
                    </label>
                </div>

                <div
                    class="collapse mt-1"
                    id="price_history"
                >
                    <input
                        id="price_history_url"
                        type="hidden"
                        value="/chairs/{{ chair.id }}/price-history"
                    />

                    <button
                        class="btn btn-outline-primary btn-sm"
                        title="Reload price history"
                        type="button"
                    >
                        Reload price history
                    </button>
                </div>
            </div>

            <div class="card-footer">
                <a href="{{ path('furniture_chair_index') }}" class="btn btn-secondary">
                    back to list
                </a>

                <a href="{{ path('furniture_chair_edit', {'id': chair.id}) }}" class="btn btn-primary">
                    edit
                </a>

                {{ include('furniture/chair/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
