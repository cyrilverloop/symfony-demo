{% extends 'base.html.twig' %}


{# SEO #}

{% set pageHeadTitle = 'Edit chair' %}


{# Assets #}

{% block importmap %}
    {{ importmap(['app', 'product-delete', 'product-edit']) }}
{% endblock %}


{# Body #}

{% block main %}
    <div class="container">
        <h1>Edit chair</h1>

        {{ include('furniture/chair/_form.html.twig') }}

        <a href="{{ path('furniture_chair_index') }}" class="btn btn-secondary">back to list</a>

        {{ include('furniture/chair/_delete_form.html.twig') }}
    </div>
{% endblock %}
