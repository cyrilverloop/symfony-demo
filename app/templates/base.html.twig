{% extends "@CVTemplatesBundle/layout.html.twig" %}


{% set viewport = 'width=device-width, initial-scale=1' %}

{% block favicon %}{% endblock %}


{# SEO #}

{% set metaDescription = false %}


{# Assets #}

{% block importmap %}
    {{ importmap('app') }}
{% endblock %}


{# Body #}

{% block body %}
    {{ parent() }}

    {# Si l'on a besoin du loader #}

    {% if loader is defined and loader == true %}
        {{ include('loader.html.twig', sandboxed = true, ignore_missing = false) }}
    {% endif %}
{% endblock %}
